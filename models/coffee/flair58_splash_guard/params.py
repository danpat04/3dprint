"""flair58_splash_guard — 공통 파라미터.

좌표계 (조립 기준):
  원점 = 실리콘 매트의 뒤쪽 모서리 한쪽
  x : 매트 폭 방향 (0~300)      y : 앞쪽 (사용자 방향)      z : 위
  벽 안쪽 면이 매트 가장자리와 일치한다 → 옆벽 안쪽 x=0 / x=300, 뒷벽 안쪽 y=0

  ※ 좌우 주의: 사용자는 앞(+y)에서 뒤(-y)를 바라본다. 따라서
    **x=300 쪽이 사용자 기준 왼쪽**, x=0 쪽이 오른쪽이다. 전원선 홈은 x=300 쪽.
"""

# ---- 벽 ----
WALL_T = 2.5           # 벽 두께 (0.5 압출폭 기준 5줄)
WALL_H = 200.0         # 뒷벽·옆벽 기본 높이
INNER_W = 300.0        # 안쪽 폭 = 매트 폭

# ---- 옆벽 사다리꼴 ----
SIDE_L = 200.0         # 옆벽이 앞으로 뻗는 길이
TAPER_Y = 150.0        # 여기까지 WALL_H, 여기서부터 낮아짐
FRONT_H = 100.0        # 앞 끝(y=SIDE_L) 높이

# ---- 발 ----
# 바깥 플랜지는 앞/뒤를 따로 둔다 (사용자 피드백).
#   옆 = 0  — 눈에 보이고 지저분해서 제거. 없어도 옆으로 57.9° 는 버틴다
#   뒤 = 유지 — 벽에 가려 안 보이고, 가장 약한 방향(뒤로 넘어짐)을 보강한다.
#              첫 레이어 접지 면적도 넓어져 출력 안정성이 오른다
# 뒤 플랜지는 캔틸레버다 — 굽힘 모멘트가 뿌리에서 최대, 끝에서 0 이므로
# **뿌리는 두껍고 끝은 얇은 쐐기**가 등응력 형상에 가깝다. 균일 두께는 끝살이
# 일을 안 하면서 무게만 차지한다. 접지 면적은 쐐기든 균일이든 동일하다
# (경사는 윗면만 깎는 것이라 바닥에 닿는 넓이는 그대로).
FOOT_BACK = 20.0       # 뒷벽 뒤쪽 플랜지 폭 (매트 뒤 조리대 여유 5cm+ 확인됨)
FOOT_SIDE = 0.0        # 옆벽 바깥 플랜지 폭 — 제거 (보기에 지저분)
FOOT_OUT_T = 4.0       # 플랜지 뿌리(벽쪽) 두께
FOOT_TIP_T = 1.2       # 플랜지 끝 두께 — 0 으로 보내면 한두 레이어로 말리고 부서진다
FOOT_IN = 10.0         # 안쪽 혀 폭 (매트 밑)
FOOT_IN_T = 1.5        # 안쪽 혀 두께
FOOT_RAMP = 5.0        # 혀 끝 램프 길이 (매트가 완만히 올라타게)

# ---- 전원선 홈 (왼쪽 옆벽에만) ----
CORD_Y = 50.0          # 뒷벽 안쪽 면(y=0)에서 앞으로  (x=300 쪽 = 사용자 왼쪽 벽)
CORD_W = 8.0           # 홈 폭 (전선 Ø6.5, 최대 36° 기울기 허용)
CORD_DEPTH = 30.0      # 상단에서 아래로 파는 깊이

# ---- 중앙 이음 (텅 & 그루브) ----
SPLIT_X = INNER_W / 2  # 150 — 뒷벽 중앙에서 분할
# 텅을 **역사다리꼴(도브테일)** 로 만들어 x 방향으로 빠지지 않게 한다.
# 뿌리(x=SPLIT_X)보다 끝(x=SPLIT_X+TONGUE_L)이 넓어 그루브 입구를 통과할 수 없다.
# 조립은 위에서 아래로 수직 슬라이드만 가능해진다.
BOSS_T = 8.0           # 이음부 벽 두께 (바깥으로 두껍게, 안쪽 면은 평평 유지)
BOSS_L = 12.0          # 이음부 보강 길이 (x)
TONGUE_ROOT = 2.5      # 텅 뿌리 두께
TONGUE_TAPER = 0.75    # 한쪽당 벌어지는 양 (끝으로 갈수록)
TONGUE_L = 10.0        # 텅 돌출 길이
FIT = 0.3              # 끼움 공차 (한쪽당)

# 파생
TONGUE_TIP = TONGUE_ROOT + 2 * TONGUE_TAPER      # 4.0 — 끝 두께
GROOVE_ROOT = TONGUE_ROOT + 2 * FIT              # 3.1 — 그루브 입구
GROOVE_TIP = TONGUE_TIP + 2 * FIT                # 4.6 — 그루브 안쪽
GROOVE_L = TONGUE_L + FIT                        # 10.3
SHOULDER = (BOSS_T - GROOVE_TIP) / 2             # 1.7 — 그루브 가장 넓은 곳 양옆 살
BOSS_Y = -BOSS_T / 2                             # 보스 두께 중심 (텅/그루브 중심선)
